services:
  news-agent:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./report:/app/report
      - ./logs:/app/logs
      - ./cache.json:/app/cache.json
    restart: unless-stopped
